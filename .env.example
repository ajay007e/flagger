# Used by docker-compose.yml (copy to .env to change any of these).
# Every value has a default, so this file is optional.
# If you change credentials or ports, update backend/.env to match.

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=flagger
MYSQL_USER=flagger
MYSQL_PASSWORD=flagger
MYSQL_PORT=3306

REDIS_PORT=6379
